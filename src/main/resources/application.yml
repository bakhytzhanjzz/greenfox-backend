spring:
  datasource:
    url: jdbc:postgresql://dpg-d3cplefdiees7386b67g-a:5432/greenfox_db_w1vy  # Internal DB URL
    username: greenfox_db_w1vy_user
    password: E7tSsGttgc34362OfLJZ6HgYviB0qOyd
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update  # Let Hibernate auto-update the schema
    flyway:
      enabled: true  # Enable Flyway migrations if you're using them

  data:
    redis:
      host: greenfox-redis  # Replace with the internal Redis hostname
      port: 6379

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace
