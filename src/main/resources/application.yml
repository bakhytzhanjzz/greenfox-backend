spring:
  datasource:
    url: jdbc:postgresql://dpg-d3ca538gjchc73918nkg-a:5432/greenfox_db  # Internal DB URL
    username: greenfox_db_user
    password: I5RvshR1jiRXbHYLjsISPeLWYWnYl68P
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update  # Let Hibernate auto-update the schema
    flyway:
      enabled: true  # Enable Flyway migrations if you're using them

  data:
    redis:
      host: greenfox-redis  # Replace with the internal Redis hostname
      port: 6379

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace
